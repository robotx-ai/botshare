interface:
  display_name: "Hifivebot Web"
  short_description: "Hifivebot standards: terminology, neutral theme, provider ownership rules, Supabase safety checks"
  default_prompt: "Use $bot-sharing-web to enforce Hifivebot service rental standards, provider-owned service and booking access rules with admin oversight, normalize UI colors to white/gray/black, and run Supabase-safe policy checks."

policy:
  allow_implicit_invocation: true
