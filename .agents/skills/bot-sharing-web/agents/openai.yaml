interface:
  display_name: "BotShare Web"
  short_description: "BotShare standards: terminology, neutral theme, provider ownership rules, Supabase safety checks"
  default_prompt: "Use $bot-sharing-web to enforce BotShare service rental standards, provider-owned service and booking access rules with admin oversight, normalize UI colors to white/gray/black, and run Supabase-safe policy checks."

policy:
  allow_implicit_invocation: true
